---
title: Confidential Bundle
description: How the signed Stogas confidential gateway evidence bundle is structured.
---

The confidential bundle is the public trust set for gateway nodes. Clients verify it, begin fetching a replacement 60 to 75 seconds before its signed expiry, and keep using the current bundle until a newer one verifies. They accept only nodes whose TLS keys, certificate hashes, report data, and quotes appear in the verified bundle.

## Files

Control publishes two object types:

| Object           | Purpose                                                                     |
| ---------------- | --------------------------------------------------------------------------- |
| Immutable bundle | Versioned evidence file, such as `bundles/v/42/bundle.json`.                |
| Latest bundle    | Stable full bundle copy at `bundles/latest.json` for efficient CDN serving. |

Control uploads and verifies the immutable object before conditionally replacing `bundles/latest.json` with the same signed envelope. An older publisher cannot regress the latest sequence.

## Bundle Body

Each bundle contains:

- schema, sequence, creation time, expiry time, and TTL
- the active and rollback IGVM/release slots from Control policy, including descriptive release metadata, launch measurement, IGVM SHA-256, launch policy values, Stogas signature, and GitHub in-toto evidence
- one entry per currently trusted gateway node
- AMD verifier evidence and exact chip/TCB collateral when available
- Control body checksum and signature

Node entries bind:

- node ID derived from the in-guest boot identity
- chip ID and exact reported TCB policy evidence
- TLS public key hash
- accepted certificate hash slot or slots
- HPKE public key
- Ed25519 response-proof public key
- report-data values and report-data hash
- full quote and quote verification timestamp

The report-data values are the public preimage for the SEV-SNP `REPORT_DATA` hash. They include `schema`, `catalog_hash`, `tls_spki_sha256`, `active_cert_sha256`, `accepted_cert_sha256`, `hpke_public_key`, `ed25519_public_key`, and the current drand Quicknet beacon.

Vendor collateral entries expose active KDS/verifier material by type, chip scope, source URL, fetch time, SHA-256, and payload. Clients that verify raw quotes can use the bundled collateral or fetch vendor collateral independently.

The release stack has at most two slots. It is the active/rollback policy, not just a summary of the currently live nodes. Control refuses to publish a node whose quoted release measurement is outside this stack. `launch_policy.vcpu_count` describes the number of measured virtual-processor states in the IGVM; it does not prove host CPU placement. Memory size and socket/core/thread topology are infrastructure configuration and are intentionally absent from attestation evidence.

The high-level shape is:

```json
{
	"body": {
		"schema": "stogas.confidential-bundle.v1",
		"sequence": 42,
		"created_at": "2026-07-06T12:00:00.000Z",
		"expires_at": "2026-07-06T12:10:00.000Z",
		"ttl_ms": 600000,
		"allowed_igvms": [
			{
				"launch_policy": {
					"schema": "stogas.gateway.launch-policy.v1",
					"sequence": 42,
					"igvm_sha256": "sha256...",
					"measurement": "sha384...",
					"name": "relay-4c",
					"release_tag": "vX.Y.Z",
					"source": {
						"repository": "https://github.com/StogasAI/gateway",
						"commit": "git-sha...",
						"tree": "git-tree..."
					},
					"vcpu_count": 4,
					"launch": {
						"policy": "0x0000000000030000",
						"host_data": "0000...",
						"vmpl": 0,
						"family_id": "0000...",
						"image_id": "0000...",
						"id_key_digest": "0000...",
						"author_key_digest": "0000..."
					}
				},
				"github_in_toto": [],
				"stogas_signature": {
					"schema": "stogas.gateway.signature.v1",
					"algorithm": "Ed25519",
					"key_id": "stogas-release-key...",
					"signed": "gateway-launch-policy.json",
					"signature": "base64url..."
				}
			}
		],
		"nodes": [
			{
				"node_id": "node...",
				"chip_id": "chip-id...",
				"reported_tcb": "reported-tcb...",
				"release_measurement": "sha384...",
				"quote": "base64url...",
				"report_data_sha512": "sha512...",
				"report_data": {
					"schema": "stogas.node-report.v1",
					"catalog_hash": "sha256...",
					"tls_spki_sha256": "sha256...",
					"active_cert_sha256": "sha256...",
					"accepted_cert_sha256": ["sha256..."],
					"hpke_public_key": "base64url...",
					"ed25519_public_key": "base64url...",
					"drand": {
						"network": "quicknet",
						"chain_hash": "52db9ba70e0cc0f6eaf7803dd07447a1f5477735fd3f661792ba94600c84e971",
						"round": 123456,
						"randomness": "hex...",
						"signature": "hex..."
					}
				}
			}
		],
		"vendor_collateral": []
	},
	"body_sha256": "sha256...",
	"signature": {}
}
```

## Cert Hash Slots

Most nodes have one accepted certificate hash. During ACME renewal a node temporarily publishes two:

| Slot   | Meaning                                        |
| ------ | ---------------------------------------------- |
| Active | Certificate currently served by the gateway.   |
| Next   | Renewed certificate allowed before switchover. |

Control begins renewal 30 days before expiry. The node switches only after every still-valid bundle containing it accepts the new hash. The old hash is pruned after the replacement is active and every still-valid bundle can authenticate it. The 48-hour window is an emergency readiness cutoff, not the normal renewal start.

## Refresh Policy

Gateway quotes refresh every 10 seconds. Each quote refresh attempts a verified drand Quicknet fetch with a 4-second total budget and 2-second per-relay request deadlines. Nodes heartbeat every 60 seconds. Control checks the bundle every 3 minutes and republishes when evidence changes, collateral changes, the refresh interval elapses, or the latest bundle approaches expiry.

Clients use signed `expires_at` as the deadline, start fetching a replacement 60 to 75 seconds beforehand, and fail closed at expiry if no replacement can be verified. This is deadline-driven and does not require fixed polling. Bundle validity is at most 10 minutes and is shortened to the earliest included identity, certificate, or AMD evidence deadline. A verified generation remains publishable through transient heartbeat loss for up to 24 hours and is omitted once less than 5 minutes of that identity window remains. Control does not use guest heartbeat health as a load-balancing signal. A new node receives a traffic-admission lease only after it is fully provisioned, appears in the latest bundle, and every older unexpired bundle that excludes it has expired.
