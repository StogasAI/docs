---
title: Responses
description: >-
  Create an OpenAI-compatible Responses API response. The gateway resolves the
  requested model and route through the compiled Stogas catalog, applies
  deployment facts such as implied service tier, rejects unsupported parameters,
  and then relays the request to the configured provider.
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Create an OpenAI-compatible Responses API response. The gateway
          resolves the requested model and route through the compiled Stogas
          catalog, applies deployment facts such as implied service tier,
          rejects unsupported parameters, and then relays the request to the
          configured provider.
---

<APIPage document={'stogas'} operations={[{ path: '/v1/responses', method: 'post' }]} />
